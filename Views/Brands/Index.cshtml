@model IEnumerable<ShoesStore.Models.Brand>

@{
    ViewData["Title"] = "Index";
}

<link rel="stylesheet" href="~/css/home.css" />

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nume)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nume)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id_Brand">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id_Brand">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id_Brand">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
